query ($login: String!) {
    user(login: $login) {
        starredRepositories {
            edges {
                node {
                    nameWithOwner
                    description
                    url
		    id
                }
            }
        }
    }
}
